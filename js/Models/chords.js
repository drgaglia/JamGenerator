
class Chord {
    constructor ( notes, key ) {
        this.notes = notes;
        this.key = key;

        if ( key ) {
            // Make chord in reference to a given key
        } else {
            // Make chord 'in a vacuum'
        }
    }

}
